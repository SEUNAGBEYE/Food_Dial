# from Homepage.models import Product

# class Cart():
# 	itemsName = None
# 	itemsQuantity = 0

# 	def __init__(self, oldcart):

# 		if oldcart:
# 			self.itemsName = oldcart.itemsName
# 			self.itemsQuantity = oldcart.itemsQuantity

# 	def addItem(self, Product, Product.id):
# 		itemToAdd = {'name': Product.name, 'price': Product.price, 'quantity': 0, 'id':Product.id}

# 		if self.itemsName:
# 			if Product.id in itemsName:
# 				itemToAdd = self.itemsName[Product.id]
		
# 		itemToAdd['quantity'] +=1
# 		itemToAdd[Product.id] = Product.id
# 		self.itemsName[Product.id] = itemToAdd
# 		itemsQuantity+=1
